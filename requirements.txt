# Core Django and Production Dependencies
Django==5.2.4
djangorestframework==3.16.0
gunicorn==20.1.0
whitenoise==6.9.0

# Database
psycopg2-binary==2.9.10
dj-database-url==3.0.1

# Environment and Configuration
python-decouple==3.8
python-dotenv==1.1.1

# Authentication and Security
django-allauth==65.10.0
django-cors-headers==4.3.1
PyJWT==2.10.1
cryptography==45.0.5

# Ecommerce essentials
django-filter==25.1
django-countries==7.6.1
stripe==7.7.0  # For payments (alternative to Paystack)

# File Storage and Media
cloudinary==1.44.1
django-cloudinary-storage==0.3.0
Pillow==10.0.0

# Cache and Background Tasks
redis==4.6.0
celery==5.5.3
django-celery-beat==2.6.0
django-celery-results==2.5.0

# Chatbot AI/ML (Essential only - lightweight)
google-generativeai==0.8.5
google-api-python-client==2.176.0
google-auth==2.40.3
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.0.0

# Lightweight NLP for chatbot
nltk==3.9.1
sentence-transformers==2.2.2  # For semantic search
transformers==4.30.2  # For text processing
torch==2.7.1  # Required for transformers (CPU only)

# Audio Processing for voice chatbot
SpeechRecognition==3.10.0
gtts==2.3.2
pydub==0.25.1

# HTTP and API clients
requests==2.32.4
aiohttp==3.8.6
beautifulsoup4==4.12.3

# Data processing for ecommerce
pandas==1.5.3  # For product analytics
numpy==1.24.3  # Required for ML libraries

# Search functionality
whoosh==2.7.4  # Lightweight search engine for products

# Essential utilities
python-dateutil==2.8.2
pytz==2022.7.1
certifi==2025.7.9
urllib3==2.5.0

# Core dependencies
asgiref==3.9.1
sqlparse==0.4.3
tzdata==2025.2

# Ecommerce specific
python-slugify==8.0.1  # For SEO-friendly URLs
django-mptt==0.16.0    # For category hierarchies
django-taggit==5.0.1   # For product tags

# Payment processing
paystack==1.6.1  # Since you're using Paystack

# Removed heavy dependencies that cause slowness:
# tensorflow==2.12.0          # Too heavy - use transformers instead
# rasa==3.6.16                # Too heavy - build custom chatbot
# spacy==3.5.3                # Too heavy - use transformers
# cities_light==3.10.1        # Heavy geographic data
# channels==4.3.1             # WebSocket - add only if needed for real-time chat
# scikit-learn==1.1.3         # Add only if doing ML recommendations
# matplotlib==3.5.3           # Add only if generating charts